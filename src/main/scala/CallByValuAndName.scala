object
CallByValuAndName {

  /*
  call by values is used default in scala
  call by reference ->
  def costOne(x: Int, y: => Int)   -> second argument is call by name

  call by value may lead to infinite loop
  call by name -> functions and function applications





   */
}
